# Maple Mono, cut down to ASCII (WOFF2)

The same subset as `tsgraph/fonts`, in the format a browser wants, so the pages
read in the face the drawings are lettered in whether or not the reader has the
font installed. Provenance, rebuild command and licence: `tsgraph/fonts/`.
