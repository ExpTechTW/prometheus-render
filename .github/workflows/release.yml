name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0 # so the release notes can span back to the last tag

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: Test before publishing
        run: go test ./...

      - name: Build every target
        run: hack/build-release.sh "${GITHUB_REF_NAME}"

      # Kept alongside the Release itself, so a failure in the step below does
      # not mean building all eighteen targets again.
      - name: Upload the builds
        uses: actions/upload-artifact@v7
        with:
          name: prometheus-render-${{ github.ref_name }}
          path: dist/
          compression-level: 0

      - name: Publish
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "${GITHUB_REF_NAME}" \
            --title "${GITHUB_REF_NAME}" \
            --generate-notes \
            --verify-tag \
            dist/*
